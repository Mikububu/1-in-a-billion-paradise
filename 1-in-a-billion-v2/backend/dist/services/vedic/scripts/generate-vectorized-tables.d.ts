/**
 * VECTORIZED TABLE GENERATOR
 *
 * Generates static 2D lookup tables for O(1) vectorized matching.
 * Uses the verified scoring engine as the source of truth.
 */
export {};
//# sourceMappingURL=generate-vectorized-tables.d.ts.map